name: generic-amd64
vendor: Sergio Schvezov <sergio.schvezov@canonical.com>
icon: meta/ubuntu.png
version: 1.4
type: oem

config:
    ubuntu-core:
        autopilot: true

oem:
    branding:
        name:  amd64
        subname: generic

    hardware:
        bootloader: grub
        architecture: amd64
        partition-layout: minimal
        boot-assets:
            files:
                - path: grub.cfg
