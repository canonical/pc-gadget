name: pc
version: '16.04-0.9'
type: gadget
base: core20
summary: PC gadget for generic devices – arm64 version
description: |
    This gadget enables generic pc devices to work with Ubuntu Core
grade: stable
confinement: strict
icon: icon.png

parts:
  grub-prepare:
    plugin: nil
    stage-packages:
      - grub-efi-arm64-signed
      - grub-efi-arm64-bin
      - shim-signed
    prime: [ -* ]
  grub:
    source: .
    build-packages:
      - grub-common
    plugin: make
    after: [grub-prepare]

